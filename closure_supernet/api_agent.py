"""Published Supernet entrypoint.

The visible application remains the minimal translational projection. The one
additional endpoint evaluates reopening, rule-chart, trading, resource and
legacy instantiations through the same source-return/re-closure equation. It is
a pure reading and creates no parallel mutation or truth runtime.
"""

from .api_interactive_translation import app, create_app


__all__ = ["app", "create_app"]
