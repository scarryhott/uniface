"""Published Supernet entrypoint.

The production app exposes one closure ball whose UI, admissible hair actions,
maze/AI reading, curvature/token reading, and closure certificate are equal
translations of the same interaction event.  The existing source-preserving
return remains the sole mutation route.
"""

from .closure_ball_runtime import app, create_app


__all__ = ["app", "create_app"]
