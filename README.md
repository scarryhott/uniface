Live field (join here): https://uniface-supernet-production.up.railway.app

A second person joins the Supernet by opening that URL and interacting. The repository is a chart; the living field is the source-preserving interaction. TRUE is not issued merely because a local form returns.

# Uniface / Closure Supernet

**Closure Supernet is one continuous integrator of human, agent, resource,
translation, selection, action, return, reopening, physical-sensor, and
first-person forms into one append-only relational field.**

```text
exact source or returned form
→ integrate
→ relation sensing
→ explicit witnesses
→ natural form where rigid
→ path–partition
→ action / return
→ successor potential
→ reopening
```

Closure is not an imposed protocol, quotient, idempotent limit, class-count
endpoint, projective fold, AI verdict, token gate, or selected presentation. It
is derived when source-preserved visual existence supplies compatible
translational-truth equations, those equations become visual axioms, and a
separate closure-explicit meeting admits them. The quotient, monoid modality,
0↔∞/tan reading and token relations may then be consequences of that closure.

## Alpaca through NRRF879–881

The default `closure-supernet-alpaca` entrypoint now runs through the explicit
NRRF879–881 runtime correspondence bridge. The Alpaca adapter remains source-only:
it collects, signs and persists one market's quote, trade, order and cumulative
fill states. `trading_temporal_market_closure` derives the single-market temporal
closure, TT partition, hair horizon, returned ball and OPEN field. The new
`trading_nrrf879_881_runtime_bridge` then projects those already-derived objects
into the NRRF881 digital environment/action/UI surface without defining a new
closure law, selector or profit model.

The live path is therefore:

```text
Alpaca source events
→ signed append-only source history
→ single-market temporal closure
→ current TT natural form
→ NRRF880 support / horizon / OPEN action field
→ NRRF881 market/account/action/UI projection
```

The bridge exposes the environment action families `OBSERVE`, `WAIT`, `MARKET`,
`LIMIT`, `CANCEL`, `REPLACE`, and `NOOP`; price and quantity are environment
coordinates, not truth selectors, and no automatic order submission is enabled.
It fails OPEN if runtime FIFO/LIFO matching, next-bid exit semantics,
instantaneous ask/bid closure, multi-asset closure requirements, or automatic
execution reappear. The source-only collector remains available separately as
`closure-supernet-alpaca-source-only`.

The runtime records correspondence to
`NRRF879SingleMarketTemporalTradingClosureDerivedFromTranslationalTruth`,
`NRRF880TemporalTradingTruthHorizonSupportActionAndTheAntiSmugglingTheorems`, and
`NRRF881DigitalMarketEnvironmentAdversarialityAndTheUIDerivedFromTheClosureBall`.
It deliberately reports `lean_kernel_executed_by_runtime=false`: Python does not
re-prove Lean and theorem names are not runtime semantic authority.

Install and run:

```bash
pip install -e '.[alpaca]'
closure-supernet-alpaca
```

Use `closure-supernet-alpaca --loop --interval 15` to continue receiving source
returns. The adapter requires its Ed25519 public key in
`CLOSURE_SOURCE_WITNESS_PUBLIC_KEYS`; its private key stays with the adapter.
A SQLite source-event ledger supplies persistent replay protection. Missing
returned fees leave net execution OPEN. The adapter and the formal bridge never
submit orders automatically.

The semantic user interface is the perspective visualization that presents
translation equations as truth constraints and returns their closure-transformed
natural forms. Each receipt carries the pre-axiometry visual mirror and its
post-closure `interface_natural_form` as two moments of one mechanism. HTML,
Canvas and CSS are transport only; they do not introduce semantic nodes,
phases, actions, gates, or equality.

See the versioned runtime modules and formal proof index for the remaining Supernet architecture.
