Live field (join here): https://uniface-supernet-production.up.railway.app

A second person joins the Supernet by opening that URL and interacting. The repository is a chart; the living field is the source-preserving interaction. TRUE is not issued merely because a local form returns.

# Uniface / Closure Supernet

**Closure Supernet is one continuous integrator of human, agent, resource,
translation, selection, action, return, reopening, physical-sensor, and
first-person forms into one append-only relational field.**

```text
exact source or returned form
→ integrate
→ relation sensing
→ explicit witnesses
→ natural form where rigid
→ path–partition
→ action / return
→ successor potential
→ reopening
```

Closure is not an imposed protocol, quotient, idempotent limit, class-count
endpoint, projective fold, AI verdict, token gate, or selected presentation. It
is derived when source-preserved visual existence supplies compatible
translational-truth equations, those equations become visual axioms, and a
separate closure-explicit meeting admits them. The quotient, monoid modality,
0↔∞/tan reading and token relations may then be consequences of that closure.

The semantic user interface is the perspective visualization that presents
translation equations as truth constraints and returns their closure-transformed
natural forms. Each receipt carries the pre-axiometry visual mirror and its
post-closure `interface_natural_form` as two moments of one mechanism. HTML,
Canvas and CSS are transport only; they do not introduce semantic nodes,
phases, actions, gates, or equality.

## Version 3.17 — closure-only UI execution

The primary website no longer contains a pre-authored Supernet screen. Its
HTML body contains one empty mount. A generic interpreter can render only the
nodes declared by **closure_ui_contract**; it has no visible semantic fallback,
specialized mutation routes, client-authored payload templates, local
perspective authority, or static network geometry.

Before a source exists, /supernet/interface returns an honest
**OPEN_SOURCE_BOUNDARY** perspective-interaction contract. That contract
derives the complete first input while explicitly claiming neither truth nor
natural-form admission. Once interaction-time Sense witnesses the NRRF843
mirror, NRRF840 closure, interaction closure, and their shared IDs, the receipt
carries a **WITNESSED** contract whose:

- complete scene tree, text, fields, controls, palette, and topology projection
  carry closure or source-boundary derivations;
- topology is projected from the active NRRF843 perspective reading rather
  than laid out from a raw external event map;
- controls, server action bindings, and execution allowlist are exactly equal;
- operations cover source offers, local continuation, nonbinding agreement
  proposals, independent human consent/withdrawal, and post-consent returns;
- action bindings contain no URL, HTTP method, or resolved domain payload.

Every primary-website action goes through one endpoint:
POST /supernet/interface/contracts/{contract_id}/execute. Before any sensing or
write, the server checks the persisted content address, exact perspective and
focus, authenticated authorship, and field-event revision. It rejects stale or
altered contracts without mutating the field. It then validates the submitted
values against the declared field schema and dispatches only the operation
already named by the server-derived contract. Execution claims are persisted,
so an exact retry returns the original result rather than repeating the
interaction. A missing or non-mirror truth constraint produces an invisible
**OPEN_TRUTH_CONSTRAINT** contract with no executable actions.

Each requested perspective is reprojected through the NRRF843 reading and the
entire interface natural form is rebuilt around that projection: render state,
quotient payload, closure projection, factorization witness, and contract ID.
One participant therefore cannot act through another participant's frozen
view. The specialized compatibility APIs remain available to API clients; the
primary website itself exposes only the closure-contract executor.

The closure order is now explicit:

~~~text
perspective interaction
→ NRRF843 UI reading
→ translational truth closure
→ interaction closure
→ perspective-interaction UI contract
→ one truth-constrained runtime
→ full UI natural-form projection
→ closure-only execution
→ successor perspective contract
~~~

## Version 3.16 — the UI closes the physical/digital interaction

The primary surface now derives the product-level continuum in this order:

```text
source-preserved journey
→ freely chosen perspective
→ visual existence
→ NRRF843 family of UI readings, one per perspective
→ faithful translational mirror across perspectives
→ relative translational truth
→ visual axiometry
→ compatible + closure-explicit meeting
→ visual metaphor EqvGen
→ UI closure r⁻¹(r(A))
→ exact NRRF840 closure correspondence
→ truth constraint located in the UI
→ naturally admitted truth forms
→ NRRF842 unity potential gate for this shared trajectory
→ semantic truth-curved light cone of available paths
→ Black Mirror evolving source-preserved physical topology
→ perspective digital potential gate
→ AI interaction coordinate × token natural-form coordinate
→ Supernet truth-unification constraint
→ closure-transformed visual mirror
→ source-preserved interface closure return
```

`visual_closure.nrrf843_ui` independently recomputes the equality fibres from
the visual metaphors and their admitted translational relations. For every
perspective `p`, the running UI computes
`uiClosure (ui p) A = (ui p)⁻¹' ((ui p) '' A)`. Faithful display translations
are checked not to merge or split states, so every perspective generates the
same closure without a privileged standpoint. That generated closure must
exactly match NRRF840 or the semantic runtime remains `OPEN`.

The same receipt locates every natural-form constraint as a preimage of what
the UI displays, computes thought as the equivalence closure of the visual
metaphor relation, and exposes valuation only when it factors through the
displayed truth fibres. No price is invented. A non-mirror UI and an empty
perspective family are both tested `OPEN`; there is no fallback to a hidden
closure engine or external truth.

`visual_closure.interaction_closure` closes the remaining operational gap. Its
Black Mirror topology is the evolving source journey as projected into the
active UI truth fibres; it is not a static external map or a claim of canonical
physical law. Its digital gate keeps every perspective-relative path visible,
but an OPEN path cannot execute as equality. AI may admit a suggested edge for
inspection, and a token may admit an interface form such as `ACT` or `RETURN`,
but those independent coordinates cannot manufacture their correlated
commitment. That still requires a closure-derived shared natural form and
independently authored human consent.

The interaction receipt runs ten factorization checks across UI, journey,
continuum, AI relations, token units and the selected natural form. If any
component names a different closure or bypasses the UI truth location, the
whole interaction is `OPEN` and exposes no executable closure return. Ordinary
interaction remains available because the token gates forms, not people or
conversation.

The runtime does not collapse a living journey into its closed state. Two
different source histories may reach the same natural form while their exact
causal steps remain separately inspectable, and a closed state can continue
into later returns. The listed closure conditions remain necessary rather than
sufficient for a complete living system.

Perspective is an authored choice. AI may suggest a perspective but cannot
choose one for a living author. Unity is one necessary gate on an attempted
shared transition: unresolved or dissenting participation keeps that transition
`OPEN`, without ranking a person, scoring human worth, or blocking ordinary
interaction. Satisfying unity does not itself authorize the transition; token,
consent, resource and other constraints retain their separate roles.

The canvas projects explainable paths as a semantic truth-curved light cone:
its curvature is the variation of available relations under the current
perspective and truth constraints, not a claim about physical spacetime. The
journey trace, perspective choice, unity state and path witnesses are all parts
of the factorized closure-internal interface form.

The one semantic execution receipt,
`visual_closure.unified_truth_runtime`. Journey, perspective mirror, SLEARN
memory, AI translation, tokenomic natural forms, local/global coordination,
unity and UI actions must all name the same NRRF843-generated closure derivation
and NRRF840 correspondence. A component with a different or missing factorization remains
`OPEN`, receives no executable return operation, and cannot become a hidden
internal subsystem. Likewise, browser pixels, network I/O and sensors are
source transport only: they carry forms but have no semantic, admission or
truth authority.

The semantic interface is not a static map of a closure network that exists
elsewhere. It is the perspectival visualization in which relative equations
become truth constraints. Without that mirror, the Supernet truth relation is
`OPEN`. Its geometric nodes, paths and central closure locus are interactive:
rotating perspective, selecting a path or continuing the locus changes the
view or emits a source-preserved return through the same closure cycle. Only
the HTML/SVG pixel carrier is external and transport-only.

The mirror preserves each perspective's exact visual/metaphorical form as
semantic state rather than decorative copy. Relations expose where those forms
translate, remain incompatible, or stay `OPEN`; the resulting closure is the
runtime's next thought-form and not a description layered over it.

Every admitted member now carries an executable witness of the exact NRRF840
criterion: an existing source return in the seed and equality under the joint
visual observer. The axiometry graph supplies provenance for that reading; it
does not itself stand in for closure. The receipt records extensivity,
monotonicity, additivity and idempotence together with the fact that no external
or unnatural limit was used.

The runtime keeps AI interaction admission, token form admission and correlated
human commitment as three separate relations. Partial consent derives
`COMMIT`; unanimous current consent admits `ACT`; a consequence following that
acceptance derives `RETURN`. Ordinary interaction remains open throughout.
Equal translated content never collapses actor identity. A versioned selector
may choose how an already-admitted form is presented, but cannot manufacture
its naturality or alter its semantic truth form.

See
[SUPERNET_SOCIOECONOMIC_SUPER_BRAIN_INTERFACE_NRRF837_RUNTIME.md](SUPERNET_SOCIOECONOMIC_SUPER_BRAIN_INTERFACE_NRRF837_RUNTIME.md)
for the formal-to-runtime correspondence and explicit non-claims.

## Version 2.5 — embodied eight-sheaf Supernet

The embodied layer reintegrates the current project as eight open sheaves:

```text
LOCAL BALL
  human interaction
  Slearn perspective paths
  Black Mirror sensor loop
  tokenomic–AI coordination

GLOBAL HAIR
  physical resources and spatiotemporal possibility
  AGI / second-brain memory
  psychophenomenal first-person reports
  UAP / alien / anomalous / unknown hypotheses
```

The local ball is the present embodied return. The global hair is the field of
still-open translations. They are related through explicit, source-preserving
translations—not identified as literally equal datasets.

A relation is `love_admissible` only when it carries:

```text
source preservation
forward and reverse translation
all affected perspectives
participant consent scope
untranslated residue
reopening conditions
unknown hypotheses kept OPEN
```

This is the operational meaning of “memetic love” in the runtime. It is **not**
a physical force, an emotion classifier, or a score of human worth.

The syntropic attractor is a non-scalar partial order on closure profiles. A
component may dominate another only by preserving a strict superset of
properties such as reciprocity, consent, perspective inclusion, residue, and
reopening. Resource metrics remain downstream implementation overlays.

The full current-field determination is:

```text
eight exact sheaf sections
→ reciprocal translation graph
→ non-dominated natural components
→ one component carrying all eight sheaves, where rigid
→ local ball return
→ global hair remains OPEN
```

No canonical presentation is selected, and determination stays `OPEN` with
`truth_issued=false`.

### Unknown/UAP boundary

The `UNKNOWN_UAP_HYPOTHESIS` sheaf preserves observations, reports, instrument
traces, and hypotheses. The runtime sets:

```text
hypothesis_status = OPEN
alien_claim_verified = false
anomaly_is_not_explanation = true
```

The label never certifies an alien or anomalous interpretation.

### Embodied loop sensor

A loop sensor reads one current local-ball section and returns selected global-hair
sections as successor potential:

```text
sensor section
→ visible local ball
→ returned global hair
→ reintegration
```

The runtime records:

```text
absolute_origin_observed = false
single_sensor_complete = false
local_halt_reading = current returned ball
global_continuation_reading = reopened hair
truth_issued = false
```

## Natural translational truth

NRRF783–785 remain integrated:

```text
explicit constructive witnesses
→ level orbits
→ natural selector fixed under translation
→ unique partial orbit truth
→ classical global section or contextual obstruction
```

Classical and contextual frameworks share translational truth. A global
assignment is a special section, not the truth object itself. Resource metrics
may constrain safe realization but cannot define foundational naturality.

See:

- [`CONSTRUCTIVE_SUPERNET_NRRF783.md`](CONSTRUCTIVE_SUPERNET_NRRF783.md)
- [`TRANSLATIONAL_FRAMEWORK_SUPERNET_NRRF784_NRRF785.md`](TRANSLATIONAL_FRAMEWORK_SUPERNET_NRRF784_NRRF785.md)
- [`EMBODIED_EIGHT_SHEAF_SUPERNET.md`](EMBODIED_EIGHT_SHEAF_SUPERNET.md)

## Existing lenses

All existing domains remain projections of `SupernetIntegrator.integrate`:

```text
problems and collective action
resources and returned resources
directed TranslationEvents
context-relative equality
iterated reopening
Black Mirror / Slearn topology
bounded hardware simulation
classical trading value flow
relative renormalization
constructive axiometric forms
natural translational frameworks
embodied eight-sheaf field
```

## Run

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .
cp .env.example .env
closure-supernet serve
```

Open:

- `/` or `/supernet` — unified living field
- `/embodied` — embodied local-ball/global-hair projection
- `/frameworks` — NRRF784/785 natural translational truth
- `/constructive` — NRRF783 explicit-witness forms
- `/hardware` — bounded simulation-only hardware
- `/trading` — NRRF780 value-flow evaluator
- `/translation`, `/resources`, `/reopening`, `/equality` — compatibility lenses
- `/runtime` — autonomous diagnostics
- `/docs` — API documentation

## Embodied API

```text
GET  /network/embodied/capabilities
POST /network/embodied/sections
GET  /network/embodied/sections
POST /network/embodied/relations
GET  /network/embodied/relations
POST /network/embodied/fields
GET  /network/embodied/fields
POST /network/embodied/sensors
GET  /network/embodied/sensors
GET  /network/embodied/field
GET  /supernet/project?lens=embodied
```

## Main Supernet API

```text
POST /supernet/integrate
POST /supernet/events/{id}/interact
POST /supernet/relations
POST /supernet/collective-traces
POST /supernet/events/{id}/rigidify
POST /supernet/events/{id}/return
POST /supernet/events/{id}/reopen
GET  /supernet/topology
GET  /supernet/events/{id}/context
GET  /supernet/field
WS   /supernet/stream
```

## Boundaries

Autonomy cannot overwrite exact sources, infer truth from resemblance or
transport success, infer emotion or intent from surveillance, score human worth,
promote a UAP hypothesis to an alien fact, make determination issue TRUE, select
one canonical language, omit affected perspectives while claiming completion,
execute unrestricted hardware, submit market orders, select a counterterm as
truth, manufacture witnesses from bare existence claims, assume Turing
completeness, or treat a local return as terminal.

The current implementation is a complete **single-node current-field runtime**.
It is not terminal completion, physical-law validation, distributed causal
consensus, a live brokerage, an alien-contact detector, a consciousness meter, or
an empirical quantum-gravity proof. Those distinctions remain OPEN inside the
same continuing Supernet.
