# Full Supernet closure: the relative natural-form potential gate

The runtime no longer identifies Supernet with its presently witnessed equality closure. That closure remains the truth constraint inside the larger local gate:

```text
Supernet(p,t)
  = RelativeNaturalFormPotentialGate(
      witnessed truth,
      OPEN continuation,
      perspectival transport,
      localities,
      all retained natural-form potentials,
      hair,
      zoom,
      return-consequence maze,
      unitary curvature,
      exact return fidelity,
      path history
    )
```

Navigation transports the current standpoint and rederives the complete local gate while preserving one `truth_invariant_id`. A source-preserving return alone may change that truth identifier and therefore rederive the gate, maze, curvature and natural-form solution.

The public surface is the current local natural form of this complete gate. A visible path is not a graph decoration: it is exactly one of `PERSPECTIVE_TRANSPORT`, `LOCALITY_TRANSPORT`, or `OPEN_RETURN_EXTENSION`.

AI and token remain two phases of the same path curvature. OPEN paths carry anticipatory curvature; returned paths carry committed curvature. Neither rendering nor selection can witness equality.
