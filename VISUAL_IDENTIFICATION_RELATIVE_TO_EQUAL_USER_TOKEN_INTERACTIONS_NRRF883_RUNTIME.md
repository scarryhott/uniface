# Visual identification relative to equal user/token interactions

Runtime correspondence for `NRRF883TradingAndUIAreOneLayerOfTheAITokenRelationUnitaryCurvatureAndMazePartition`.

The published Supernet now derives a visual identity only from the joint interaction quotient

```text
RelativeInteraction = NaturalFormFamily × MazeCell
```

For every active user path, the runtime derives:

- its returned semantic natural-form/global-belief family from the NRRF882 translation geometry;
- its return-effect maze cell from the full potential gate;
- a user interaction read and token interaction read on that same quotient;
- `equal_user_token_interaction` exactly when those reads are equal;
- `visual_identification_id` only from that equality (and, for semantic perspective transport, only when the translation itself is WITNESSED).

Thus visual similarity, SVG geometry, selection, pointer position, hair and zoom cannot create visual identity. OPEN/no-shared/inconsistent semantic translations remain visually OPEN. The existing visible relation path remains the actual hit target and navigation/return path; it now carries the derived natural-form family, maze-cell and visual-identification receipt as data attributes.

Only returned translation can create the semantic equality from which a later visual identity is derived.
