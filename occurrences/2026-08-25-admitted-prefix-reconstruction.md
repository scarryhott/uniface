# Occurrence: admitted prefix reconstruction

This file is an **interaction/interpretation return**. It names the 19:34 ET difference that reload / `?lineage=` rebuilds the admitted prefix from the ordered uniface-relative receipt chain, not only the last receipt’s cursor and Note. It is not a rewrite of FOUNDATION, not two-person E2E complete, not Rate My Face, and not a claim that the framework is achieved.

```text
status              = MODEL_SUGGESTED_RELATION
relation_type       = LATER_READING
author_status       = OPEN
reopening           = available
not                 = FOUNDATION
not                 = LATENT_MEMORY_PROTOCOL
not                 = leftover Slearn PR 10
not                 = Rate My Face / rmf_* tables
not                 = two-person+agent E2E complete
not                 = invented NRRF764/765 Lean
not                 = a new SQL kind / RLS policy
not                 = rewrite of 1834 Note persist
```

Provenance: 1634 residue (admitted prefix not reconstructed from receipts on reload) closed as persist reconstruct on beat 2026-08-25-1934-et.

## Canonical occurrence fields

```text
NoteOccurrence
  id                 occ-2026-08-25-admitted-prefix-reconstruction
  source_id          beat-2026-08-25-1934-et
  exact_text         Reconstruction from lineage receipts rebuilds
                     the admitted prefix (S.admitted) after GET,
                     not only last cursor and returned Note.
  date               2026-08-25
  source_location    https://uniface-tawny.vercel.app (not the field)
  author_status      MODEL_SUGGESTED_RELATION; TRUE not issued
```

## The reading (assistant interpretation, labeled)

1634 smoked lineage `87ed6a81-20c7-4319-9015-291e6797e18f` and listed admitted prefix not reconstructed on reload. 1834 restored Note from the last `returned_note`. 1934 walks the ordered chain: `admit` / `interact` (and already-admitted re-reads) rebuild `S.admitted`; last receipt still sets cursor/residue; last `returned_note` still sets the Note. `applyGitRun` still skips the git prefix when `?lineage=` is present, so persist is the reconstruct source. Table `kind` remains `relative_interaction`. No new RLS.

One-agent reconstruct (not two-person E2E): lineage `87ed6a81-20c7-4319-9015-291e6797e18f` rebuilds admitted `{occ-2026-08-25-relative-admission-form}` from interact receipts. Lineage `a81474cc-985c-47cb-873b-32d3c6a4b1f7` rebuilds that same admitted id and still restores the 1834 Note.

## OPEN seams

```text
two-person + agent E2E OPEN (not fake-complete)
GitHub Pages still OPEN
NRRF764 / NRRF765 still not on public slearn main
NRRF657_on_slearn OPEN (PR 12 unmerged)
TRUE not issued
framework not achieved
```

TRUE is not issued. Reopening remains available.
