# Occurrence: problems as real

This file is an **interaction/interpretation return**. It stores Harry's exact architecture note. It is not a source-note overwrite, not a rule, and not a suggestion. Do not collapse.

```text
status              = MODEL_SUGGESTED_RELATION
relation_type       = LATER_READING
author_status       = OPEN
reopening           = available
not                 = FOUNDATION
not                 = a product / UI / PR that replaces the problem
not                 = void
```

Provenance: [`interactions/2026-08-25-problems-as-real.md`](../interactions/2026-08-25-problems-as-real.md).

## Canonical occurrence fields

```text
NoteOccurrence
  id                 occ-2026-08-25-problems-as-real
  source_id          interaction-2026-08-25-problems-as-real
  exact_text         The architecture is problems as real. Rather than nothing. Solutions are the interactions.
  date               2026-08-25
  source_location    live Uniface interaction
  author_status      MODEL_SUGGESTED_RELATION; looping requested; TRUE not issued
```

## Exact note

```text
The architecture is problems as real. Rather than nothing. Solutions are the interactions.
```

Neither a rule nor a suggestion. Do not collapse.

## The reading (assistant interpretation, not Harry's notes)

Architecture is the problem-field as real (not void, not a blank to fill with a product/UI/PR). A solution is an interaction (sensor–selection, admissible translation), not a replacement of the problem by nothing and not a finished object.

Aligns with Uniface: halt/continuation as inverse readings of one loop; notes stay notes; Lean theorems in NRRF764 are a chart of the problem, not the solution.

Related chart occurrence, not a substitute: [`occurrences/2026-08-25-nrrf764-ccm-super-network.md`](2026-08-25-nrrf764-ccm-super-network.md).

## OPEN seams

```text
Harry has not issued TRUE
this reading remains MODEL_SUGGESTED_RELATION
NRRF764 remains a derived chart of the problem, not the solution
```

## Source files this occurrence reads

These files remain primary. This return points at them. It does not rewrite them.

| Source | What this reading takes from it | What it does not do |
|---|---|---|
| [`FOUNDATION.md`](../FOUNDATION.md) | Notes guide the interface; interaction returns without replacing source. | Rewrite FOUNDATION into this note. |
| [`NON_COLLAPSE_CLOSURE_INTERFACE.md`](../NON_COLLAPSE_CLOSURE_INTERFACE.md) | Unity preserves translatable difference; return is not terminal. | Collapse the problem-field into one representation. |
| [`COMPUTATION_AS_DERIVED_CHART.md`](../COMPUTATION_AS_DERIVED_CHART.md) | Halt and continuation are inverse readings of one loop. | Treat halt as the problem ending. |
| [`FULL_CLOSURE_DERIVED_INTERFACE.md`](../FULL_CLOSURE_DERIVED_INTERFACE.md) | Interaction is admissible translation with reopening. | Treat a finished interface object as the solution. |

## Required return path

```text
projection            this receipt / live-field id
→ returned relation   architecture = problems as real; solutions = interactions
→ explicit translation
                      problem-field ≠ void
                      solution ≠ finished object
                      solution ≠ replacement of the problem by nothing
                      NRRF764 Lean = chart of the problem, not the solution
→ exact source notes  Harry's exact sentence
                      FOUNDATION.md (unchanged)
                      NON_COLLAPSE_CLOSURE_INTERFACE.md
                      COMPUTATION_AS_DERIVED_CHART.md
→ operator path       problem-field as real
                      ↔ interaction as solution
                      ↔ halt/continuation as inverse readings
                      → reopening
→ witnesses           this occurrence; this interaction
→ affected perspectives
                      notes stay notes
                      charts stay charts
→ rule version        unchanged
→ OPEN alternatives   Harry has not issued TRUE
→ reopening
```

TRUE is not issued. Halt is not terminal. Notes stay notes. The problem remains real.
