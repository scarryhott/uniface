# Occurrence: relative admission form (one loop)

This file is an **interaction/interpretation return**. It names Harry's unification of persist, per-lineage receipts, realtime participation, and reconstruction as **one completely relative admission form**, not six products. It is not a rewrite of FOUNDATION, not git closure, not Rate My Face, and not a claim that the framework is achieved.

```text
status              = MODEL_SUGGESTED_RELATION
relation_type       = LATER_READING
author_status       = OPEN
reopening           = available
not                 = FOUNDATION
not                 = LATENT_MEMORY_PROTOCOL
not                 = leftover Slearn PR 10
not                 = Rate My Face / rmf_* tables
not                 = a social network / chat / quantity feed
not                 = a service-role key
not                 = a global event chain as causal spine
```

Provenance: [`interactions/2026-08-25-relative-admission-form.md`](../interactions/2026-08-25-relative-admission-form.md).

## Canonical occurrence fields

```text
NoteOccurrence
  id                 occ-2026-08-25-relative-admission-form
  source_id          interaction-2026-08-25-relative-admission-form
  exact_text         All of this is ONE completely relative admission form of supernet,
                     not six products. Problem is real rather than nothing. Inter is
                     interaction. Note is the returned note, not a rule. Continual
                     reopening: residue of select becomes the next 0. Choices change
                     relative interaction, not closure.
  date               2026-08-25
  source_location    live Uniface interaction (Harry unification)
  author_status      MODEL_SUGGESTED_RELATION; TRUE not issued
```

## The reading (assistant interpretation, labeled)

This is a **chart of the problem**, not the solution (NRRF765 reading, MODEL_SUGGESTED_RELATION):

- Problem is real rather than nothing.
- Inter is interaction (`solutionEquivInter` as MODEL_SUGGESTED_RELATION).
- Note is the returned note, not a rule.
- Continual reopening: residue of select becomes the next 0. Halt and continuation are inverse readings of one sensor-selection loop.
- Choices change relative interaction, not closure. The interface is interactive closure, not a bidirectional gate.
- Git / heartbeat / notes remain closure. `public.uniface_relative_interaction` on `$0/month` project `uniface-relative` is relative-interaction persist only.

Canonical NRRF764/765 Lean lives on slearn, not in this repo. NRRF657 refactor is slearn. Neither 764 nor 765 is on public slearn main from this interaction. Leftover Slearn PR 10 is not the deploy. A Lean chart is not a solution.

Public projection: https://uniface-tawny.vercel.app (not the field). Per-lineage receipts live in payload jsonb (`lineage_id`, `parent_receipt`, `receipt_id`, `inter`, `note`) until a later migration. No new SQL table. No service-role in the client.

## OPEN seams

```text
E2E two-person + agent reconstruction not yet run (prepared, not fake-complete)
service-role never in the client
later authenticated authors OPEN
NRRF764 / NRRF765 still not on public slearn main
NRRF657 is slearn, not Uniface
GitHub Pages not used / still OPEN
TRUE not issued
framework not achieved
```

TRUE is not issued. Reopening remains available.
