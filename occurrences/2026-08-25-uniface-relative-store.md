# Occurrence: uniface-relative persist (not closure)

This file is an **interaction/interpretation return**. It names Harry's confirmed $0/month Supabase project as the relative-interaction persist of the live Uniface projection. It is not a rewrite of FOUNDATION, not git closure, and not Rate My Face.

```text
status              = MODEL_SUGGESTED_RELATION
relation_type       = LATER_READING
author_status       = OPEN
reopening           = available
not                 = FOUNDATION
not                 = LATENT_MEMORY_PROTOCOL
not                 = a closure beat
not                 = leftover Slearn PR 10
not                 = Rate My Face / rmf_* tables
not                 = a service-role key in the repo
```

Provenance: [`interactions/2026-08-25-uniface-relative-store.md`](../interactions/2026-08-25-uniface-relative-store.md).

## Canonical occurrence fields

```text
NoteOccurrence
  id                 occ-2026-08-25-uniface-relative-store
  source_id          interaction-2026-08-25-uniface-relative-store
  exact_text         Harry confirmed: new $0/month Supabase project uniface-relative
                     in org Harry's projects, separate from Rate My Face. It exists
                     and is ACTIVE_HEALTHY. Wire the live Uniface projection so
                     select/admit persists as relative interaction only. Git remains
                     closure.
  date               2026-08-25
  source_location    live Uniface interaction (Harry confirm)
  author_status      MODEL_SUGGESTED_RELATION; TRUE not issued
```

## The reading (assistant interpretation, labeled)

Harry confirmed a new $0/month project named `uniface-relative` (id `thpzzkaymledzcwsfqhn`, region us-east-1, org `vercel_icfg_cwyRW9MMhui7trzdO1PskZsJ`), ACTIVE_HEALTHY, separate from Rate My Face (`supabase-coquelicot-candle` / `clqsfnmlyobbduavetpk`).

Table already created with RLS: `public.uniface_relative_interaction`. This table is **not** closure, **not** FOUNDATION, **not** a beat, **not** Rate My Face. Select/admit/residue on `docs/index.html` POSTs `kind=relative_interaction` with publishable/anon only. No service-role key is fetched, stored, or committed.

Git remains closure. The public URL https://uniface-tawny.vercel.app remains a projection, not the field. The interface remains interactive closure, not a bidirectional gate.

## OPEN seams

```text
service-role never in the client
whether later authenticated authors should exist OPEN
NRRF764 / NRRF765 still not on public slearn main
GitHub Pages not used / still OPEN
TRUE not issued
framework not achieved
```

TRUE is not issued. Reopening remains available.
