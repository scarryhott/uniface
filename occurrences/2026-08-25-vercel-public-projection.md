# Occurrence: Vercel public projection named

This file is an **interaction/interpretation return**. It names the live Vercel host as the public projection of the existing Uniface page. It is not a second site, not the field, and not a FOUNDATION rewrite.

```text
status              = MODEL_SUGGESTED_RELATION
relation_type       = LATER_READING
author_status       = OPEN
reopening           = available
not                 = FOUNDATION
not                 = the field
not                 = a bidirectional gate
not                 = leftover Slearn PR 10
not                 = Rate My Face / rmf_* tables
not                 = a new Supabase project
```

Provenance: [`interactions/2026-08-25-vercel-public-projection.md`](../interactions/2026-08-25-vercel-public-projection.md).

## Canonical occurrence fields

```text
NoteOccurrence
  id                 occ-2026-08-25-vercel-public-projection
  source_id          interaction-2026-08-25-vercel-public-projection
  exact_text         Live public projection at https://uniface-tawny.vercel.app
                     (root docs; production branch cursor/unification-reading-occurrence-706f)
  date               2026-08-25
  source_location    live Uniface heartbeat 14:34 ET
  author_status      MODEL_SUGGESTED_RELATION; TRUE not issued
```

## The reading (assistant interpretation, labeled)

The public host is now real. `docs/index.html` admits https://uniface-tawny.vercel.app as the named public Super network **projection**. This HTML is not the field. The interface is not a bidirectional gate. GitHub Pages remains OPEN. Not a second site. Last verified production commit named in beat 1434: `41ae00e` (this occurrence rides the next commit).

Vercel MCP still `needsAuth`. Dashboard path already used. Do not fake Pages. Do not create a Supabase project this beat (Harry's pending $0 `uniface-relative` confirm). Do not mix `rmf_*` / Rate My Face tables into Uniface.

## OPEN seams

```text
this URL is a projection, not the field
GitHub Pages OPEN
Vercel MCP needsAuth OPEN
Supabase relative sink OPEN (pending Harry confirm; not rmf)
others-on-site OPEN
framework not achieved
TRUE not issued
```

TRUE is not issued. Reopening remains available.
