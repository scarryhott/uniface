# Occurrence: loop-sensor integration

This file is an **interaction/interpretation return**. It records the digital Super network surface running the notes' loop of sensor and selection. It is not a source note, not FOUNDATION, and not the network.

```text
status              = MODEL_SUGGESTED_RELATION
relation_type       = LATER_READING / FRAME_TRANSLATION
author_status       = OPEN
reopening           = available
artifact            = docs/index.html
not                 = FOUNDATION
not                 = a halt oracle
not                 = leftover Slearn PR 10
not                 = the network
```

Provenance: [`interactions/2026-08-25-loop-sensor-integration.md`](../interactions/2026-08-25-loop-sensor-integration.md).

## Exact demand

```text
Integrate that loop sensor the notes describe of the digital Supernet.
```

## The reading (assistant interpretation)

One continuous interactive loop:

```text
sense  : S → Obs
select : S → Obs → S′
```

`S` is the loop state: selected point / `0`, live occurrence field, OPEN seams.

`Obs` is what sense reads from `S`: shown, hidden, OPEN, current occurrence, determined prefix, residual continuation.

`select` takes `S` and `Obs` and returns `S′`: a new selected `0` and residual continuation. Each user step is one interaction.

One run is shown split:

```text
determined prefix     = halt as a reading
residual future       = continuation
```

Same run, two inverse interpretations. If continuation is constant from some time, that is the halt reading of this split. Never: the loop has enumerated all selections; never: halt decided from a finite horizon (`no_universal_selection`, `no_finite_halting_test`).

Problems remain real after a step. OPEN seams are not deleted by selecting. The page is a projection, not the field (`O_B ≡ R` refused).

`r`/`i` remain the local geometric chart. Sensor–selection is the loop, not a third slider.

## OPEN seams

```text
TRUE not issued
no halt oracle
no Turing completeness
kantian-ivi unreachable
Aristotle native equality OPEN
NRRF764 not verified on public slearn main
leftover Slearn PR 10 is not the deploy
```

## Required return path

```text
projection            docs/index.html loop sensor
→ returned relation   digital Super network surface runs sense/select
→ explicit translation
                      halt ↔ continuation = inverse readings of one run
                      solutions = interactions
                      problems remain real
                      this HTML ≠ the network
→ exact source notes  FOUNDATION.md (unchanged)
                      COMPUTATION_AS_DERIVED_CHART.md
                      AXIOMETRIC_SOURCE_GRAMMAR.md loop–sensor–selection
                      occ-2026-08-25-problems-as-real
                      occ-2026-08-25-nrrf764-ccm-super-network
→ operator path       S —sense→ Obs —select→ S′ → reopening
→ witnesses           this occurrence; docs/index.html
→ rule version        unchanged
→ OPEN alternatives   listed above
→ reopening
```

TRUE is not issued. Notes stay notes. This HTML is not the network.
