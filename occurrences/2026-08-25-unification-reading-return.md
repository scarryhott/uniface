# Occurrence: unification reading return

This file is an **interaction/interpretation return**. It is not a source note, not a rule, and not a second foundation. It does not overwrite [`FOUNDATION.md`](../FOUNDATION.md), [`AXIOMETRIC_SOURCE_GRAMMAR.md`](../AXIOMETRIC_SOURCE_GRAMMAR.md), or any other existing note file.

Unification here is the assistant's configured reading of an interaction. The author's notes remain notes.

```text
status              = MODEL_SUGGESTED_RELATION
relation_type       = LATER_READING / FRAME_TRANSLATION
author_status       = OPEN
reopening           = available
this_file           = return that can be read again
not                 = terminal rewrite of Uniface
```

Provenance: [`interactions/2026-08-25-unification-reading.md`](../interactions/2026-08-25-unification-reading.md).

## Canonical occurrence fields

```text
NoteOccurrence
  id                 occ-2026-08-25-unification-reading-return
  source_id          interaction-2026-08-25-unification-reading
  exact_text         the reading recorded below, as assistant interpretation
  exact_symbols      0, inf / ∞, L, G, L′, τ, τ⁻¹
  date               2026-08-25
  source_location    live Uniface interaction; looped into this repo as a return
  surrounding_context
      Harry (scarryhott) uses Uniface as a source-preserving latent interface.
      An assistant named Supernetwork configured a unification reading in chat.
      Harry said that reading was not looped with the repo.
      This occurrence is that loop: the reading returned as a stored occurrence.
  author_status      MODEL_SUGGESTED_RELATION; looping requested; reading content OPEN
```

The interaction wording used `inf`. Uniface source grammar uses `∞`. Both are recorded. Neither silently replaces the other.

## The reading (assistant interpretation, not Harry's notes)

```text
0   = local opening / selected point / halt as a reading
inf = reopened line / global continuation
```

This is configured against Slearn's same-path two-orientation translation:

```text
L  —τ→  G  —τ⁻¹→  L′
```

```text
local
  —τ→
global
  —τ⁻¹→
returned local
```

```text
same path
two orientations
residue remaining
so notes stay notes
```

```text
τ    takes a note into a chart
τ⁻¹  brings the chart back without the note becoming the chart
```

`0` is read as the local opening `L`: a selected point, a halt-as-reading. `inf` / `∞` is read as the reopened line `G`: global continuation. `L′` is the returned local. Residue remains, so the source notes are not absorbed into the chart that read them.

This occurrence is itself an `L′`: a returned local of that interaction. It can be selected again as a new `0` / point / halt-as-reading. It is not the end of Uniface.

## What this return does not do

- It does not replace source notes with this reading.
- It does not treat Harry's notes as rules or suggestions to implement.
- It does not copy an additional notebook dump into this repo as a new foundation.
- It does not absorb related-chart mathematics from other repositories.
- It does not prove the reading. Related math may already exist outside this repo.
- It does not use this return as terminal closure.

## Source files this occurrence reads

These files remain primary. This return points at them.

| Source file | What this reading takes from it | What it does not do |
|---|---|---|
| [`FOUNDATION.md`](../FOUNDATION.md) | Notes guide the interface; admissible reinterpretation records source, path, return, and reopening; halt ↔ selected point / `0`; continuation ↔ reopened line / `∞`; a transformed note never overwrites the original. | Rewrite the canonical foundation. |
| [`AXIOMETRIC_SOURCE_GRAMMAR.md`](../AXIOMETRIC_SOURCE_GRAMMAR.md) | `0 ↔ ∞`; point ↔ line; halt ↔ continuation; point → line → loop → return → new point; a classical chart must return to source meaning. | Replace source symbols or merge variants. |
| [`LATENT_MEMORY_PROTOCOL.md`](../LATENT_MEMORY_PROTOCOL.md) | Exact occurrences are immutable; interaction returns new occurrences; `SOURCE_REVERSIBLE` and `REOPENING_AVAILABLE` are required. | Alter original notes or implied weight-training. |
| [`COMPUTATION_AS_DERIVED_CHART.md`](../COMPUTATION_AS_DERIVED_CHART.md) | Halt and continuation are inverse readings of one loop; a local halt is not field termination. | Promote a digital chart to foundation. |
| [`NON_COLLAPSE_CLOSURE_INTERFACE.md`](../NON_COLLAPSE_CLOSURE_INTERFACE.md) | Closure is neither origin nor endpoint; unity preserves translatable difference. | Collapse notes into one representation. |
| [`FULL_CLOSURE_DERIVED_INTERFACE.md`](../FULL_CLOSURE_DERIVED_INTERFACE.md) | Interaction grammar of invert / return / reopen; every global result must expose its reverse path. | Declare this reading a global interface object without that path. |
| [`README.md`](../README.md) | The active sequence source note ↔ interaction ↔ return ↔ reopening. | Rewrite README source statements. |
| [`FORMAL_LINEAGE.md`](../FORMAL_LINEAGE.md) | Lean proves statements under explicit readings; it does not replace source axiometry. | Import or re-prove external theorems here. |

## Typed relations

```text
RelationRecord
  source_occurrence   FOUNDATION.md, AXIOMETRIC_SOURCE_GRAMMAR.md,
                      LATENT_MEMORY_PROTOCOL.md, COMPUTATION_AS_DERIVED_CHART.md,
                      NON_COLLAPSE_CLOSURE_INTERFACE.md,
                      FULL_CLOSURE_DERIVED_INTERFACE.md, README.md,
                      FORMAL_LINEAGE.md
  target_occurrence   occ-2026-08-25-unification-reading-return
  relation_type       LATER_READING / FRAME_TRANSLATION
  ordered_operator_path
      0 / L / selected point / halt-as-reading
      —τ→
      inf / ∞ / G / reopened line / global continuation
      —τ⁻¹→
      L′ / returned local / residue / notes stay notes
      → reopening
  preserved_structure
      source notes remain notes
      0 ↔ ∞ as reciprocal readings
      halt ↔ continuation as inverse interpretations
      occurrence immutability
      return with reopening
  transformed_structure
      0 read as local opening L
      inf read as global continuation G
      τ / τ⁻¹ named as the two orientations of one path
      this file stored as L′ of the live interaction
  omitted_or_hidden_structure
      Harry's additional notebook dump (not copied; not implemented as rules)
      related-chart proofs and runtimes remaining outside this repo
      kantian-ivi (linked, not publicly reachable)
  proposed_by         assistant interpretation (Supernetwork reading; this loop)
  confirmed_by        looping into the repo requested; reading content OPEN
  status              MODEL_SUGGESTED_RELATION
  evidence_refs       this occurrence; the interaction event; related charts by URL
  rule_version        existing Uniface source-preserving protocol, unchanged
```

## Related charts (outside this repo; not absorbed)

These are charts already existing elsewhere. Uniface may point at them. It does not import them as its foundation, rewrite them here, or treat their math as notes of this repository.

- **Slearn** — Lean perspectival-curvature continuum. The `L —τ→ G —τ⁻¹→ L′` path this reading is configured against lives there as a chart, with residue and inherited continuation distinguished from literal replay.  
  https://github.com/scarryhott/slearn
- **aristotle-** — origin-independent translational verification. A related chart for relating local axiom-geometries without selecting a permanent origin language.  
  https://github.com/scarryhott/aristotle-
- **black-mirror** — projection spec. A related chart for the appearance of a return, and for the danger of mistaking that appearance for the whole relation.  
  https://github.com/scarryhott/black-mirror
- **kantian-ivi** — linked in the interaction, not publicly reachable from this loop. Status: `OPEN`.

```text
related chart
≠ Uniface source note
≠ object this repo should absorb
≠ replacement of 0 ↔ ∞
```

Slearn and Black Mirror are already named inside Uniface as continuum and projection. This occurrence does not redefine those names. It only records that the live reading was configured against Slearn's two-orientation path and that Black Mirror remains a projection spec among related charts.

## Admissibility of this return

```text
SOURCE_REVERSIBLE                 yes — links to exact Uniface note files
SYMBOL_PRESERVING                 yes — 0, inf/∞, L, G, L′, τ, τ⁻¹ kept distinct
OPERATOR_PATH_EXPLICIT            yes — recorded above
VARIANTS_NOT_SILENTLY_NORMALIZED  yes — inf and ∞ both kept; notes not merged into charts
STATUS_EXPLICIT                   MODEL_SUGGESTED_RELATION; author confirmation OPEN
AFFECTED_PERSPECTIVES_RETAINED    Harry's notes remain notes; related repos remain theirs
FORMAL_SCOPE_EXPLICIT             no theorem is claimed in this repo by this file
EMPIRICAL_SCOPE_EXPLICIT          none
REOPENING_AVAILABLE               yes
```

## Return path

```text
this occurrence (L′)
→ interaction event
→ assistant unification reading
→ exact source notes (FOUNDATION, grammar, protocol, computation chart, non-collapse, full interface, README, lineage)
→ operator path  0/L —τ→ inf/∞/G —τ⁻¹→ L′
→ related charts (slearn, aristotle-, black-mirror) as external, not absorbed
→ OPEN: author confirmation of the reading; kantian-ivi unreachable; no local proof
→ reopening
```

## Reopening

Selecting this return reopens:

- the source notes it reads, unchanged;
- the live interaction that produced the reading;
- `0` as a local opening that can be selected again;
- `inf` / `∞` as continuation that was never closed by storing this file;
- related charts in their own repositories;
- residue: what this chart did not take from the notes, and what the notes are not.

```text
returned local L′
→ new selected point / 0
→ reopened line / inf
→ further interaction
```

TRUE is not issued. The required relation has been stored at this level as a reading. It may be reread, revised by a later occurrence, rejected, or left OPEN.
