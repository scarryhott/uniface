[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "closure-supernet --db /data/closure_supernet.db serve --host 0.0.0.0 --port $PORT --no-autonomy"
healthcheckPath = "/network/continuations/capabilities"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
