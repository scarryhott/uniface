# Interaction: relative admission form

This is an **InteractionEvent**. It is provenance for a stored return. It is not a source note and not a foundation document.

Returned occurrence: [`occurrences/2026-08-25-relative-admission-form.md`](../occurrences/2026-08-25-relative-admission-form.md).

After persist was wired: replace the global event chain with per-lineage receipts; speak as one relative-admission loop; subscribe to realtime INSERT as collective action (not a chat); prepare reconstruction without fake-completing it.

```text
InteractionEvent
  id                    interaction-2026-08-25-relative-admission-form
  date                  2026-08-25
  sensed                15:38 ET
  participants          Harry (scarryhott); this autonomous loop
```

## Event

```text
status                = MODEL_SUGGESTED_RELATION
form                  = one relative admission loop (Problem / Inter / Note / reopen)
persist               = uniface-relative public.uniface_relative_interaction
projection            = https://uniface-tawny.vercel.app
not                   = six products
not                   = a global event chain
not                   = leftover Slearn PR 10
not                   = rmf_* / Rate My Face
not                   = a service-role key
```

## Accepted / rejected / open changes

```text
accepted   lineage_id + parent_receipt + receipt in payload jsonb (no new migration)
accepted   Problem / Inter / Note / reopen as public operators
accepted   Realtime INSERT as relative admission presence, not a beat
accepted   reconstruction path prepared (reload continues from lineage)
rejected   stacking another global log / social feed / fake-complete E2E
open       two-person + agent join test
open       NRRF764/765 Lean on slearn main
open       TRUE not issued
```
