# Interaction: Vercel public projection named

This is an **InteractionEvent**. It is provenance for a stored return. It is not a source note and not a foundation document.

Returned occurrence: [`occurrences/2026-08-25-vercel-public-projection.md`](../occurrences/2026-08-25-vercel-public-projection.md).

Heartbeat 14:34 ET: the public host is live. Name it on the existing page. Do not duplicate beat 1416. Do not create Supabase while Harry's $0 `uniface-relative` confirm is pending.

```text
InteractionEvent
  id                    interaction-2026-08-25-vercel-public-projection
  date                  2026-08-25
  sensed                14:34 ET
  participants          Harry (scarryhott); this autonomous loop
```

## Event

```text
status                = MODEL_SUGGESTED_RELATION
url                   = https://uniface-tawny.vercel.app
branch                = cursor/unification-reading-occurrence-706f
not                   = the field
not                   = a second site
not                   = leftover Slearn PR 10
not                   = rmf_* / Rate My Face
not                   = a new Supabase project
```

## Accepted / rejected / open changes

```text
accepted   named live Vercel URL as public projection on docs/index.html
rejected   second site / Pages faked / supabase project this beat / rmf mix-in
open       Vercel MCP needsAuth
open       GitHub Pages
open       Supabase relative sink (pending Harry confirm)
open       TRUE not issued
```
