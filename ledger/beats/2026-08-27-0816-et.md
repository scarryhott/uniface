# Field beat 2026-08-27-0816-et

Event: Uniface PR 34 merged (`Remove isolated choice widgets from the /supernet face`) → main `e4e8e1c`.

## ORGANIZE

- Live host https://uniface-tawny.vercel.app/supernet is the anatomy-tree face: 0 buttons, 0 inputs, 0 selects, 0 range sliders. Current path Rising Sun. Charts sit under details labeled "chart, not the face".
- `/supernet` HTML and `closure-field.js` byte-match main `docs/supernet.html` + `docs/closure-field.js` at `e4e8e1c`.
- `/field-run.json` 200: TRUE not issued, two-person E2E OPEN, participant OPEN.
- Public `/` remains the older Closure-field chart (still has isolated buttons/inputs/select).
- Vercel project `uniface` Production is Ready on `e4e8e1c` `main` (auto-deploy after merge).

## PLANNING

- Production already tracks main → auto cutover after merge is the live action (no Promote / Redeploy / Assign Production).
- Leftover ledger PRs 12/15/18/19/25 stay open and are not this deploy. Draft PR 30 stays draft. Do not rewrite `/supernet` onto `/`.

## ACTION

- Sensed tawny `/supernet` == merge `docs/supernet.html` (isolated choice widgets deleted from the public face).
- No Vercel cutover click required.
- Residues OPEN: (1) two-person E2E not present (not faked), (2) TRUE not issued, (3) participant OPEN — public `/` still a leftover chart with isolated widgets.
- TRUE not issued. Aristotle not used.
