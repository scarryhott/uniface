# Field beat 2026-08-27-0844-et

Event: Uniface PR 35 merged (`Make / the supernet runtime; serialize advancing /field-run.json`) → main `a2d91a61`.

## ORGANIZE

- Live host https://uniface-tawny.vercel.app `/` is the widget-free Sense → unique unitary path selector → TE face (0 buttons, 0 inputs, 0 selects, 0 forms, no inlined JWT). Charts sit under details labeled "chart, not the face". `data-projection="face"`.
- Live `/` HTML byte-matches main `docs/index.html` at `a2d91a61` (sha256 prefix `ca6f992a52f1f562`, 8311 bytes). Live `/supernet` byte-matches main `docs/supernet.html` (8383 bytes, `data-projection="panzoom"`) — own pan/geometry projection, not a rewrite of `/supernet` onto `/`.
- `/field-run.json` 200, `Cache-Control: no-store`. Consecutive GETs advance (cycle 29 `te` → 30 `reopen` observed). Snapshot at beat: cycle 46, stage sense, selected contradiction, `truth_issued: false`, TRUE not issued, two-person E2E OPEN, participant OPEN.
- Vercel project `uniface` Production already served this after merge (tawny `/` etag identical to PR 35 preview `uniface-git-cursor-root-superne-f6a069-harrys-projects-08015ef2.vercel.app`).

## PLANNING

- Production already tracks main → auto cutover after merge is the live action (no Promote / Redeploy / Assign Production).
- Leftover ledger PRs 12/15/18/19/25 stay open and are not this deploy. Open PRs 1/9/30 are not this. Do not rewrite `/supernet` onto `/` (rejected PR 17).

## ACTION

- Sensed tawny `/` == merge `docs/index.html` (public root is the supernet runtime). `/field-run.json` serializes advancing `currentUnifiedField`.
- No Vercel cutover click required.
- Residues OPEN: (1) two-person E2E not present (not faked), (2) TRUE not issued, (3) participant OPEN.
- TRUE not issued. Aristotle not used.
