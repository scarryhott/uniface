# Field beat 2026-08-26-1952-et

Event: Live https://uniface-tawny.vercel.app lagged PR23 after PR24 merge `2ea346801132` on main. Cutover recorded as Domains preview alias reassignment (not Production Promote). Ledger-only.

## ORGANIZE

- Live https://uniface-tawny.vercel.app was still PR23 (`cursor/unresolved-alternatives-field-2377`: unresolved alternatives, no `isomorphismClassesOf`) while main already had PR24 merge `2ea346801132` (head `f24b6c4`, branch `cursor/isomorphism-class-selector-b141`).
- The public host is a record of that lag, not a missing merge and not a runtime rewrite in this beat.

## PLANNING

- Hobby Production Promote is capped (`api-deployments-free-per-day`).
- Cutover is Domains preview alias of `uniface-tawny.vercel.app` (project `uniface`, scope `harrys-projects-08015ef2`), not a pointer occurrence.

## ACTION

- Reassigned Preview from `cursor/unresolved-alternatives-field-2377` to `cursor/isomorphism-class-selector-b141`.
- Independent verify: live `closure-field.js` matches main (`isomorphismClassesOf` present); `/`, `/supernet`, `/field-run.json` 200; field-run has `isomorphism_classes` + `selected_class`; `truth_issued` false; `two_person_E2E` OPEN.
- Residues: two-person E2E, field-run snapshot vs live dump, restore Production after Hobby cap.
- TRUE not issued. Aristotle not used. This beat is ledger-only; FOUNDATION, `docs/index.html`, `docs/closure-field.js`, `docs/supernet.html`, and `field-run.json` are not rewritten here. `/supernet` is not rewritten to `/`.
